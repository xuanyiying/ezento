# AI Provider 配置管理系统

## 概述

AI Provider配置管理系统提供了一个完整的、基于数据库的动态配置解决方案，用于管理多个AI模型提供商的配置。

## 主要特性

### 🗄️ 数据库驱动

- 所有配置存储在PostgreSQL数据库中
- 支持动态加载和更新，无需重启应用
- 自动配置同步（多实例环境）

### 🔐 安全性

- API密钥使用AES-256-GCM加密存储
- 支持密钥轮换
- 审计日志追踪所有配置变更

### 🚀 动态管理

- 通过REST API实时管理配置
- CLI工具快速操作
- 自动检测配置变更（30秒间隔）

### 📊 监控与健康检查

- 自动健康检查（1分钟间隔）
- Provider可用性监控
- 性能指标追踪

### 💾 缓存优化

- 内存缓存提高性能
- 自动缓存失效（5分钟TTL）
- 手动刷新支持

## 架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Application Layer                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Services   │  │ Controllers  │  │     CLI      │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                  │               │
└─────────┼─────────────────┼──────────────────┼───────────────┘
          │                 │                  │
          ▼                 ▼                  ▼
┌─────────────────────────────────────────────────────────────┐
│                   Configuration Layer                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           ModelConfigService (核心服务)              │   │
│  │  • 配置加载与缓存                                    │   │
│  │  • API密钥加密/解密                                  │   │
│  │  • CRUD操作                                          │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────┴─────────────────────────────────┐   │
│  │         ConfigWatcherService (监听服务)             │   │
│  │  • 监听数据库变更                                    │   │
│  │  • 自动触发重新加载                                  │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    Provider Layer                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         AIProviderFactory (工厂服务)                 │   │
│  │  • 动态创建Provider实例                              │   │
│  │  • 健康检查管理                                      │   │
│  │  • Provider状态追踪                                  │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│         ┌─────────────┼─────────────┐                       │
│         ▼             ▼             ▼                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │  OpenAI  │  │   Qwen   │  │ DeepSeek │  ...            │
│  │ Provider │  │ Provider │  │ Provider │                  │
│  └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    Database Layer                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              PostgreSQL Database                      │   │
│  │  • model_configs (配置表)                            │   │
│  │  • audit_logs (审计日志)                             │   │
│  │  • usage_records (使用记录)                          │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 核心组件

### ModelConfigService

负责配置的CRUD操作、加密/解密、缓存管理。

**主要方法：**

- `getModelConfig(name)` - 获取配置
- `getAllModelConfigs()` - 获取所有配置
- `upsertModelConfig(config)` - 创建或更新配置
- `enableModelConfig(name)` - 启用配置
- `disableModelConfig(name)` - 禁用配置
- `deleteModelConfig(name)` - 删除配置
- `refreshCache()` - 刷新缓存

### ConfigWatcherService

监听数据库配置变更，自动触发重新加载。

**配置：**

- 检查间隔：30秒（可配置）
- 自动重新加载provider
- 手动触发支持

### AIProviderFactory

动态创建和管理AI Provider实例。

**主要方法：**

- `getProvider(name)` - 获取provider实例
- `getAvailableProviders()` - 获取可用providers
- `checkProviderHealth(name)` - 健康检查
- `reloadProviders()` - 重新加载所有providers

### ModelAdminController

提供REST API管理配置。

**端点：**

- `GET /api/admin/models` - 列出所有配置
- `POST /api/admin/models` - 创建/更新配置
- `PUT /api/admin/models/:name/enable` - 启用配置
- `PUT /api/admin/models/:name/disable` - 禁用配置
- `DELETE /api/admin/models/:name` - 删除配置
- `GET /api/admin/models/status/all` - 获取状态

## 快速开始

### 1. 安装和配置

```bash
# 设置环境变量
export DATABASE_URL="postgresql://..."
export ENCRYPTION_KEY="your-32-char-encryption-key"

# 运行迁移
npm run prisma:migrate

# 种子配置
npm run seed:models
```

### 2. 使用CLI管理

```bash
# 列出配置
npm run model-config list

# 添加配置
npm run model-config add "gpt-4o" "openai" "sk-..."

# 查看状态
npm run model-config status
```

### 3. 使用API管理

```bash
# 获取所有配置
curl http://localhost:3000/api/admin/models

# 添加配置
curl -X POST http://localhost:3000/api/admin/models \
  -H "Content-Type: application/json" \
  -d '{"name":"gpt-4o","provider":"openai","apiKey":"sk-..."}'
```

### 4. 在代码中使用

```typescript
import { AIProviderFactory } from './ai-providers/factory';

@Injectable()
export class MyService {
  constructor(private providerFactory: AIProviderFactory) {}

  async callAI(prompt: string) {
    const provider = this.providerFactory.getProvider('openai');
    const response = await provider.call({
      model: 'gpt-4o',
      prompt,
    });
    return response.content;
  }
}
```

## 文档

- **[快速入门](./QUICKSTART.md)** - 5分钟快速设置指南
- **[数据库配置](./DATABASE_CONFIG.md)** - 详细的配置说明
- **[迁移指南](./MIGRATION_GUIDE.md)** - 从YAML迁移到数据库
- **[API文档](../README.md)** - 完整的API参考

## 支持的Provider

- ✅ OpenAI (GPT-4, GPT-3.5)
- ✅ Qwen (通义千问)
- ✅ DeepSeek
- ✅ Google Gemini
- ✅ Ollama (本地部署)

## 配置示例

### OpenAI

```typescript
{
  name: "gpt-4o",
  provider: "openai",
  apiKey: "sk-...",
  endpoint: "https://api.openai.com/v1",
  defaultTemperature: 0.7,
  defaultMaxTokens: 4096,
  costPerInputToken: 0.000005,
  costPerOutputToken: 0.000015,
  rateLimitPerMinute: 3500,
  rateLimitPerDay: 200000,
  isActive: true
}
```

### Qwen

```typescript
{
  name: "qwen-max",
  provider: "qwen",
  apiKey: "sk-...",
  endpoint: "https://dashscope.aliyuncs.com/api/v1",
  defaultTemperature: 0.7,
  defaultMaxTokens: 2000,
  costPerInputToken: 0.0001,
  costPerOutputToken: 0.0002,
  rateLimitPerMinute: 2000,
  rateLimitPerDay: 100000,
  isActive: true
}
```

### Ollama (本地)

```typescript
{
  name: "ollama-llama2",
  provider: "ollama",
  apiKey: "", // 不需要
  endpoint: "http://localhost:11434",
  defaultTemperature: 0.7,
  defaultMaxTokens: 2000,
  costPerInputToken: 0,
  costPerOutputToken: 0,
  rateLimitPerMinute: 10000,
  rateLimitPerDay: 1000000,
  isActive: true
}
```

## 安全最佳实践

1. **加密密钥管理**
   - 使用强加密密钥（至少32字符）
   - 不要将加密密钥提交到版本控制
   - 定期轮换加密密钥

2. **API密钥保护**
   - 所有API密钥加密存储
   - 日志中自动脱敏
   - 限制API访问权限

3. **访问控制**
   - 使用认证中间件保护管理端点
   - 实施RBAC（基于角色的访问控制）
   - 审计所有配置变更

4. **网络安全**
   - 使用HTTPS传输
   - 配置防火墙规则
   - 限制数据库访问

## 性能优化

- **缓存策略**: 5分钟TTL，减少数据库查询
- **批量操作**: 支持批量更新配置
- **连接池**: 优化数据库连接管理
- **异步加载**: 非阻塞配置加载

## 监控指标

系统自动追踪以下指标：

- Provider可用性
- 响应时间
- 成功率/失败率
- API调用次数
- 成本统计
- 配置变更历史

## 故障排查

### Provider未加载

```bash
# 检查配置
npm run model-config list

# 检查状态
npm run model-config status

# 手动健康检查
curl -X POST http://localhost:3000/api/admin/models/health-check/openai
```

### 配置未生效

```bash
# 刷新缓存
curl -X POST http://localhost:3000/api/admin/models/refresh

# 或使用CLI
npm run model-config status
```

### API密钥错误

```bash
# 查看配置详情
npm run model-config show gpt-4o

# 更新API密钥
curl -X POST http://localhost:3000/api/admin/models \
  -d '{"name":"gpt-4o","apiKey":"new-key",...}'
```

## 开发路线图

- [ ] Web UI配置管理界面
- [ ] 配置版本控制
- [ ] 配置导入/导出
- [ ] A/B测试支持
- [ ] 配置模板系统
- [ ] 更多Provider支持
- [ ] 高级监控和告警

## 贡献

欢迎贡献！请查看 [CONTRIBUTING.md](../../../../CONTRIBUTING.md)

## 许可证

查看 [LICENSE](../../../../LICENSE)

## 支持

- 📧 Email: support@example.com
- 💬 Discord: [加入我们](https://discord.gg/...)
- 📖 文档: [完整文档](https://docs.example.com)

---

**注意**: 这是一个生产级别的配置管理系统。在生产环境使用前，请确保：

1. 使用强加密密钥
2. 配置适当的访问控制
3. 启用审计日志
4. 定期备份数据库
5. 监控系统健康状态
